<!--
  AGENTS.md — canonical agent instructions (Codex reads this directly; Claude
  Code reads it via CLAUDE.md's @AGENTS.md import; ~10 other agents read it too).

  Fill every <placeholder> from the repo scan. DELETE this comment and any
  section that does not apply. RULES:
    - Commands FIRST, with flags.                              (BP-6)
    - Specific stack + versions, not vague descriptions.       (BP-7)
    - Reference source-of-truth; never paste it.               (BP-2)
    - Stable refs only (path+symbol/route/table/command/test). (BP-3)
    - No line numbers, ever.                                    (AP-1)
    - Keep the whole root→cwd chain UNDER 32 KiB.               (AP-14)
    - No volatile/temporary detail in this always-loaded file.  (BP-11)
    - Unknowns -> "> [!WARNING] NEEDS VERIFICATION", not guesses.(BP-9)
-->
# <Project> — agent guide

<One line: concrete stack + versions, e.g. "Bun runtime (not Node); Ink+React TUI; age-encrypted vault.">

## Commands

- Install: `<cmd>`
- Test: `<cmd>` — single test: `<cmd path/to/one.test.ts>`
- Build: `<cmd>`   ·   Lint/format: `<cmd>`
- (Full list: see `package.json` scripts / `Makefile` — do not duplicate here.)

## Project structure

- `<src/...>` — <role>
- `<dir/...>` — <role>
- (Reference directories and their purpose; do not paste a full file tree.)

## Code style

- <rule> — ✅ `<good>`   🚫 `<bad>`
- <rule> — ✅ `<good>`   🚫 `<bad>`
- (Linter/formatter config is the source of truth: see `<config file>`.)

## Testing

- Framework: `<name>`. Run one test: `<cmd>`.
- <convention: where tests live, naming, fixtures — referenced, not copied>.

## Git / PR workflow

- Branch: `<convention>`. Commits: `<convention>`.
- <PR/checks expectations>.

## Boundaries

- ✅ **Always:** run tests before declaring done; <repo-specific>.
- ⚠️ **Ask first:** schema migrations; dependency bumps; <repo-specific>.
- 🚫 **Never:** commit secrets; reference line numbers; duplicate config/`package.json`; <repo-specific>.

---
Source of truth: `<dirs/files>`. Update when: `<commands/stack/version change, new guardrail, structural move>`.
